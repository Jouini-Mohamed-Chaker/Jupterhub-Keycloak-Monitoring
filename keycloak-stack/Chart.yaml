apiVersion: v2
name: keycloak-stack
description: Keycloak (codecentric/keycloakx) with a self-hosted Postgres backend for persistence
type: application
version: 0.1.0
appVersion: "26.6.2"

dependencies:
  - name: keycloakx
    version: "7.2.0"
    repository: "https://codecentric.github.io/helm-charts"
    alias: keycloak